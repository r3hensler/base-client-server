:{$PORT:80} {
    log {
        output stdout
    }

    # Backend API routes — preserve /api prefix
    handle /api/* {
        reverse_proxy {$BACKEND_URL}
    }

    # Proxy health check
    handle /proxy/health {
        respond "OK" 200
    }

    # TEMPORARY: test if public requests reach Caddy
    handle {
        respond "Hello from Caddy" 200
    }
}
