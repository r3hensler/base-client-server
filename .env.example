# ══════════════════════════════════════════════════════════════
# ⚠️  SECURITY WARNING
# ══════════════════════════════════════════════════════════════
# 1. NEVER commit this file with real credentials to version control
# 2. NEVER use default passwords (postgres/postgres) in production
# 3. ALWAYS generate strong random secrets for production
# 4. Generate passwords with: openssl rand -base64 32
# 5. Generate JWT secrets with: python -c "import secrets; print(secrets.token_urlsafe(64))"
# ══════════════════════════════════════════════════════════════

# ── Environment ──────────────────────────────────────────────
ENV=development  # Options: development, staging, production

# ── Database ─────────────────────────────────────────────────
# ⚠️  PRODUCTION: Change default postgres password!
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres  # ⚠️  CHANGE IN PRODUCTION: Use openssl rand -base64 32
POSTGRES_DB=app
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/app
TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/app_test  # Optional - auto-derived if not set

# Database Connection Pool (production defaults)
DB_POOL_SIZE=20
DB_POOL_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_ECHO=false  # Set to true to log SQL queries (verbose, exposes sensitive data)

# ── Auth ─────────────────────────────────────────────────────
# ⚠️  REQUIRED: Must be at least 32 characters
JWT_SECRET_KEY=  # Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15  # Production: 15, Development: 60 for convenience
REFRESH_TOKEN_EXPIRE_DAYS=7     # Production: 7, High-security: 1

# ── Cookies ──────────────────────────────────────────────────
# ⚠️  PRODUCTION: Must be true (requires HTTPS)
COOKIE_SECURE=true       # Set to false ONLY for local dev without Caddy
COOKIE_SAMESITE=lax
COOKIE_DOMAIN=           # Leave empty for current domain only

# ── Proxy ────────────────────────────────────────────────────
SITE_ADDRESS=localhost   # Set to your domain in staging/production
PROXY_HTTP_PORT=80
PROXY_HTTPS_PORT=443

# ── Deployment domains ───────────────────────────────────────
STAGING_DOMAIN=staging.example.com
PRODUCTION_DOMAIN=example.com
